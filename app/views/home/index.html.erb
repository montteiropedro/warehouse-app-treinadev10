<section id="warehouses">
  <h2><%= Warehouse.model_name.human(count: 2) %></h2>

  <%= link_to 'Cadastrar Galpão', new_warehouse_path %>

  <% @warehouses.each do |warehouse| %>
    <div>
      <h3><%= link_to warehouse.name, warehouse_path(warehouse.id) %></h3>

      <dl>
        <dt><strong><%= Warehouse.human_attribute_name(:code) %>:</strong></dt>
        <dd><%= warehouse.code %></dd>

        <dt><strong><%= Warehouse.human_attribute_name(:city) %>:</strong></dt>
        <dd><%= warehouse.city %></dd>

        <dt><strong><%= Warehouse.human_attribute_name(:area) %>:</strong></dt>
        <dd><%= number_with_delimiter(warehouse.area, delimiter: '.') %> m²</dd>
      </dl>
    </div>
  <% end %>
  
  <% if @warehouses.empty? %>
    <span>Não existem galpões cadastrados</span>
  <% end %>

</section>
